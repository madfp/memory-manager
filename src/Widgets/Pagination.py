"""
La paginación divide la memoria en páginas de tamaño fijo. La memoria virtual se divide en páginas, y cada página se puede almacenar en la memoria principal o en la memoria secundaria (disco duro). Cuando un proceso necesita acceder a una página que no está en la memoria principal, se produce una falla de página y la página se carga desde la memoria secundaria a la memoria principal.
"""
from PySide6.QtWidgets import QWidget

class Pagination(QWidget):
  def __init__():
    super().__init__()
    # atributes

  #methods